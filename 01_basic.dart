/**
 * main函数是入口函数，dart应用程序总是从main函数开始执行
 * 每一行代码之后，都必须要写分号
 *
 * 运行dart代码：
 * - 在命令行当中运行
 * 	dart 01_basic.dart
 * - 在Android Studio当中运行
 * 	点击main函数左边的箭头，选择Run即可
 *
 * 注释的写法：
 * // 单行注释
 * /* 多行注释 */
 * /// 文档注释，可以通过dartdoc将注释转换为文档；文档注释支持markdown语法
 *
 */

void main() {
  print("hello dart");
}
